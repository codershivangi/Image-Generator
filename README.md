# 🖼️ Image Generator  
A simple, fast, and customizable **Text-to-Image Generator** built using Python.  
This project allows users to enter a text prompt and automatically generate an image using a local ML model. Designed for beginners and developers who want a lightweight alternative to complex APIs.

---

## 🚀 Project Overview

The **Image Generator** converts text prompts into images using an open-source generative model.  
It provides:

- A clean and simple code structure  
- Local model loading (no external API needed)  
- Easy prompt input  
- Output image generation & preview  
- CPU/GPU support  

